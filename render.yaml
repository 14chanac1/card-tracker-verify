services:
  - type: web
    name: card-tracker-verify
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn ebay_verify:app
    envVars:
      - key: PORT
        value: 10000